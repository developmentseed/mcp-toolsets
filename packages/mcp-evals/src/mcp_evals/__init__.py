"""Evaluation harness for the mcp-toolsets agent.

Loads eval cases (from a Google Sheet CSV export or a local file), runs the
mcp-agent against each query, and scores its tool use and final answer.
"""
