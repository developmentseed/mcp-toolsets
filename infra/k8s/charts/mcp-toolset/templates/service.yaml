apiVersion: v1
kind: Service
metadata:
  name: {{ include "mcp-toolset.fullname" . }}
  labels:
    {{- include "mcp-toolset.labels" . | nindent 4 }}
spec:
  type: ClusterIP
  selector:
    {{- include "mcp-toolset.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
