# Optional Helm values overrides for this toolset (see charts/mcp-toolset/values.yaml).
# No secrets: the CDS API key is per calling user, sent as the x-cds-token
# HTTP header on each MCP request (see the README's "Per-user credentials").
{}
